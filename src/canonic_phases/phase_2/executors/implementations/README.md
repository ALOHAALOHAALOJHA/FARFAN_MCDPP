# Phase 2 Executor Implementations

This directory will contain 30 executor implementations (one per base question Q001-Q030).

Each executor:
- Extends `BaseExecutorWithContract`
- Implements contract validation
- Produces 10 outputs (one per policy area)
- Tracks evidence
- Reports metrics

## Status

Pending migration from `farfan_pipeline.phases.Phase_two`
